# GUI remediation — 2026-09-25

Owner: GPT-6 Sol, lane B. Baseline `0167529fa9ba81649b138c476c524dc4495d4954`; no branch, staging, commit or push action. Read `AGENTS.md`, the UI audit F3–F10, and the final remediation design before completion. Production edits are confined to `cmig/gui/app.py`, `builder.py`, `host_view.py`, `views.py`, and `cmig/service/jobrunner.py`; tests are `tests/test_app_shell.py` and new `tests/test_gui_audit_regressions.py`. Other lanes edited shared checkout files concurrently.

## Disposition

| Finding | Result and evidence |
|---|---|
| F3 | Fixed. Gene KO freezes and forwards medium with `--exact-medium`, direction and target growth fraction. Strain Growth and Ratio freeze and forward medium with exact replacement and a separately labelled cooperative tradeoff fraction; Strain Growth explicitly pins `--single-medium community`. Search target direction/growth fraction are stated inapplicable to Growth/Ratio. Request comparisons use only applicable fields; effective conditions remain in selectable run details. The new regression queues all three jobs, edits inputs before execution, checks their argv and superseded notes, and exercises Ratio through actual `main(argv)`/`cli_exact_medium`. A real one-member E. coli core Strain Growth control with seven defined uptake rows exited 0; its manifest records `exact_boundary_isolation`, `tradeoff_f=0.65`, and `single_medium_mode=community` in `.run/remediation-20260925/gui/defined-medium-control/`. No CLI medium defect was introduced. |
| F4 | Fixed for the requested offscreen geometries. Search, Host and Dynamics use scrollable content; Host and Search controls are split into shorter rows and Host graph/table panes can shrink without forcing the shell wider. Long warning/path text is held in selectable details, not in a width-setting status row. Across 17 final captures, every requested and actual window/PNG size matches exactly: nine at 1280×800 and eight at 1500×950. Minimum hints were English 1084×614 and Korean 1084×611. Active Search and Host captures at 1280 had no horizontal overflow. The full tab set, including hidden Host and advanced tabs, was instantiated. Geometry and paths: `.run/remediation-20260925/gui/geometry.json`. |
| F5 | Fixed. A nonzero Search CLI exit raises `ArtifactJobFailure`, which leaves the job `FAILED` while retaining summary, exit code and output directory in `Job.result`. The GUI registers the failed run in Explorer and shows its warnings, unevaluated candidates, specific diagnostics, scientific failed state, and artifact path in selectable details. A real licensed Gurobi run with member growth floor 100 exited 3, had two `baseline_failed` candidates, kept zero ranked rows, and reopened from Explorer; the digest remained separately `verified`. Evidence: `failed_real.json`, `en-search-failed-real-1280x800.png`, and `en-search-failed-reopened-1280x800.png` in the GUI evidence directory. The rc=2 regression verifies a failed badge, output path and actionable request checklist; parser stderr remains in the terminal because process-wide stderr redirection would corrupt concurrent jobs. Existing cancellation tests remain green. |
| F6 | Fixed. App readback uses the existing CLI `_inspect_run_dir` as the single digest authority, with strict manifest preflight. Persistent shell badge and an accessible details button distinguish `verified`, `mismatch` (including changed/missing declared artifacts), `not_recorded` with its inspector reason, and `invalid/unreadable`; scientific status is displayed separately. Tests cover valid, changed, missing-declared and legacy no-digest cases through dFBA, spatial and Host routes. Tidy and Search routes also preflight. Files above 16 MiB are hashed on a worker while the Qt event loop remains active; a timer regression verifies UI callbacks run during inspection. |
| F7 | Fixed. Figure choices are populated only from this run's existing supported SVG artifacts and cleared on invalidation. A missing or unreadable selection replaces the old preview with a specific placeholder and disables export. The selected preview and export use the same artifact name. The regression removes Scatter after Ranking loaded and checks the previous image cannot remain. |
| F8 | Fixed in the GUI. Search details are selectable and scrollable, with every warning, units/normalizer, effective conditions, candidate counts, Pareto attempt/resolved/failed counts, candidate sampling state, unevaluated diagnostic, and run path when present. Missing old fields are omitted rather than fabricated. Host warning text wraps and nullable transfer ranges/identifiability remain visible; target-search adapter displays a null transfer as unknown with range and state. The long-warning screenshots deliberately overlay four layout-stress warnings on the historical Pareto result; those four warnings are not claimed as solver output. |
| F9 | GUI part fixed. Qt SVG rendering sets `KeepAspectRatio` after each load. Wide and tall rectangle regressions measure drawn red-pixel bounds, within 0.15 of the input viewBox ratio. Actual mixed/Pareto SVG previews are shown in `en-mixed-figure-scroll-1280x800.png` and `en-pareto-figure-scroll-1280x800.png`. The historical mixed saved plot still clips the negative contribution; signed plot geometry and saved SVG/TIFF text fitting are the separate CLI/figure lane's F2/F9 work and require its new artifacts for final visual acceptance. |
| F10 | Fixed. Manifest JSON is parsed and checked before publishing a view, including object shape, nested provenance, workflow artifact list, schema version and modern digest presence. Direct summaries reject missing required dFBA/spatial fields and wrong nested types; nullable numeric values render `unknown`, not zero. Tidy bundle, graph payload and manifest are staged before UI publication. Tests confirm null/list/malformed and wrong-type readback cannot escape or leave stale tidy, dFBA or Search provenance/export state. Host keeps the complete previous valid view when a replacement fails to parse and reports the failed load. |

## Verification

| Command or probe | Outcome |
|---|---|
| `QT_QPA_PLATFORM=offscreen QTWEBENGINE_CHROMIUM_FLAGS='--disable-gpu --no-sandbox' uv run --no-sync pytest -q tests/test_gui_audit_regressions.py tests/test_app_shell.py tests/test_gui_round5_p2.py tests/test_gui_views.py tests/test_host_view.py tests/test_jobrunner.py` | **108 passed, 0 failed, 0 skipped**; `.run/remediation-20260925/gui/focused-tests.log`. Covers request forwarding, cancellation, failure readback, direct loaders and visual pixel geometry. |
| `uv run --no-sync ruff check cmig/gui/app.py cmig/gui/builder.py cmig/gui/host_view.py cmig/gui/views.py cmig/service/jobrunner.py tests/test_gui_audit_regressions.py tests/test_app_shell.py` | Exit 0, all checks passed. |
| `uv run --no-sync mypy cmig` | Exit 0, no issues in 91 source files after the science lane fixed its concurrent diagnostics. |
| `git diff --check` | Exit 0. |
| Real GUI Search run: audit E. coli core pool, size 1, target `ac`, member growth floor 100 | CLI scientific exit 3, GUI job `failed`, summary `failed`, two unevaluated candidates, zero ranked candidates, Explorer entry 1, artifact integrity `verified`; `.run/remediation-20260925/gui/failed_real.json`. |
| Real defined-medium Strain Growth control | Exit 0, summary `ok`, seven uptake rows; manifest confirms exact medium and controlled community single-medium policy. |
| Offscreen full-window capture | 17/17 exact requested/actual/PNG pixel geometry; `geometry.json` and `capture_final.py`. Visual inspection covered English and Korean empty/Host/Pareto windows at both requested sizes, real failed/reopened runs, and mixed/Pareto figure scroll states. |

The screenshots are offscreen macOS/PySide6 evidence, not native multi-monitor or HiDPI certification. Saved signed plot correction is intentionally outside GUI ownership. The coordinator owns integrated release/version, envelope, full-suite and final figure acceptance after peer edits settle.

## Post-review correction — GR-1–GR-5

The independent report `remediation_gui_review_2026-09-25.md` requested changes. This correction owns `cmig/gui/app.py`, `builder.py`, `host_view.py`, `tests/test_gui_audit_regressions.py`, and this report; it preserves the earlier F3–F10 implementation. No Git, shared environment sync, CLI/core/IO/figure, or independent-review edits were made.

| Finding | Correction and regression |
| --- | --- |
| GR-1 | The direct Host reader now skips blank point uptake in the writer's CSV and keeps sparse transfer points, ranges, and ambiguous metabolite names together. The cross-feeding table draws the union of those keys as `unknown [lo, hi]` where no point exists; identified zero stays `0`, and unknown transfers produce no invented graph edge. A regression calls the production Host output writer on the science lane's actual two-fuel objective-fixed FVA result, reads back its CSV and JSON with a verified manifest, then checks JSON-only and legacy identified-zero variants. Objective 5 and both `[0,5]` uncertainty rows survive. |
| GR-2 | Host objective and all other consumed Host point/range conversions are validated before its view changes; a rejected replacement keeps the previous Host view, run/export pointer and integrity detail and reports the rejected path. Search, dFBA, and spatial summary failures clear their views and pointers with persistent `Readback: invalid/unreadable` state while the detail retains the independent artifact-digest verdict. A missing essential tidy `nodes.parquet` is a readback failure, while a complete unsupported workflow keeps its no-viewer message. Failed Search completion now refuses an invalid manifest before publishing a pointer or Explorer entry. Tests cover verified-digest malformed summaries for all three routes, malformed Host objective after a good Host load, tidy missing nodes, and failed Search with `manifest.json=null`. |
| GR-3 | The Search view retains its displayed workflow kind and executed request, plus the applicable controls as they stood when the result appeared. An input signal invalidates only when those applicable on-screen fields change; explicit readback rejection still clears the view. Post-completion regressions cover Ratio and Strain Growth direction/target-growth edits, main Search cooperative-fraction edits, applicable edits, and an already-superseded Ratio result receiving an irrelevant direction edit. Frozen queued execution and superseded-request notes remain covered by the earlier regressions. |
| GR-4 | Selectable Search details include present `solution_semantics`, `metric`, `directions`, `weights`, `normalization_ranges`, `ga_metadata`, growth/seed conditions, the executed request, and the complete recorded summary JSON. Adjacent workflow adaptation forwards only these present source keys; absent legacy keys remain absent. A regression checks distinctive values through direct and adapted summaries alongside two warnings and partial Pareto attempt counts. |
| GR-5 | On CLI exit 2, a bounded job-boundary check reuses the actual model-pool file discovery and checks the frozen medium path. The exact missing medium or invalid pool cause appears in the failed job error and in selectable `input_diagnostic`/warning details; no process-wide stderr redirection or scientific success/ranking is introduced. Regressions run the real CLI for both errors through GUI job completion. |

Correction evidence is under `.run/remediation-20260925/gui-correction/`. With `QT_QPA_PLATFORM=offscreen` and `QTWEBENGINE_CHROMIUM_FLAGS='--disable-gpu --no-sandbox'`, `uv run --no-sync pytest -q tests/test_gui_audit_regressions.py tests/test_app_shell.py tests/test_gui_round5_p2.py tests/test_gui_views.py tests/test_host_view.py tests/test_jobrunner.py` exited 0: **122 passed, zero failed/skipped** (`focused-tests.log`). Scoped `ruff check` exited 0 (`ruff.log`); `mypy cmig` exited 0 with no issues in 91 source files (`mypy.log`); `git diff --check` exited 0 (`diff-check.log`). No layout structure changed in this correction; the prior requested 1280×800 and 1500×950 captures remain the geometry evidence, with the focused offscreen window-fit regression rerun. Independent Astra re-review and integrated release checks remain with the coordinator.

## Final GUI correction — GR-2a/GR-2b

The independent `remediation_gui_rereview_2026-09-25.md` left two material Search readback findings. This bounded follow-up edited `cmig/gui/app.py`, `cmig/gui/builder.py`, `tests/test_gui_audit_regressions.py`, and this report. It made no CLI, core, renderer, IO, dependency, CI, Git, or environment-sync change; the earlier GR-1–GR-5 and F3–F10 behavior remains in place.

| Finding | Correction and boundary evidence |
| --- | --- |
| GR-2a | Every failed Search with a dictionary result now inspects its own output directory before publication, including rc=2 with no manifest. The shared inspector reports `not_recorded` and its `no_manifest` reason for those diagnostic-only directories; the badge and detail name B before B's failed summary, current run/export pointer, and Explorer entry appear. The failed scientific status and exact input diagnostic remain visible. The two real CLI regressions load a digest-verified A first, then run missing-medium and empty-pool B, asserting B's badge/detail, both pointers, details, Explorer count, disabled figure export, and rc=2 failure. |
| GR-2b | A single Search ranking validator checks every row before `SearchView.load_summary` mutates view state. Nonobject rows and malformed present score/weighted score, target flux, target-flux map, community growth, FVA bounds, members, status/auxiliary text and diagnostics raise a readback error. Absent and null numeric readouts remain unknown; parseable legacy numeric strings remain readable. Both direct loader and completed-job success/failure paths apply the same ranking rule. With a valid digest but invalid schema, B is rejected: Search data and export/current-run pointers clear, B is not added to Explorer, and persistent `Readback: invalid/unreadable` detail retains `Artifact digest: verified` without a scientific success claim. A completed job with a nonobject result is also rejected. |

Evidence is under `.run/remediation-20260925/gui-final-correction/`. The focused offscreen GUI suite, including these good-A → bad-B real CLI and valid-digest rank-matrix cases, passed **136 tests, zero failures/skips** (`focused-tests.log`; `QT_QPA_PLATFORM=offscreen`, `QTWEBENGINE_CHROMIUM_FLAGS='--disable-gpu --no-sandbox'`, `uv run --no-sync`). The affected direct builder test file passed another 10 tests (`builder-tests.log`). Scoped Ruff passed, `mypy cmig` found no issues in 91 source files, and scoped `git diff --check` passed (`ruff.log`, `mypy.log`, `diff-check.log`). These tests verify the GUI publication boundary; independent Astra re-review and integrated release/publication gates remain with the coordinator.

## Residual Host zero readback correction — 2026-09-25

The independent scientific re-review found that an actual optimal Host LP/FVA `[0,0]` omitted acetate from the producer's positive-only map, so the saved Host view showed `unknown [0.0, 0.0]`. The Host table now identifies a finite collapsed interval directly for a known optimal host/community readout, while the network still uses the sparse positive-only map and draws no zero-width edge. The app reader passes a three-state solve trust value: explicit nonoptimal status cannot identify a point, legacy status-free summaries retain explicit finite point assertions only, and an optimal saved summary can identify a sparse zero from its interval. Broad `[0,5]` ranges override incidental point values, and invalid/nonfinite/reversed ranges are rejected before the view is changed.

The real acetate-disabled LP→production CSV/JSON→digest-verified GUI case shows table value `0`, CSV `ac,0,0,0,True`, and no acetate transfer edge despite the absent sparse-map point. The actual `[0,5]` row remains `unknown [0.0, 5.0]`; an actual FVA-failure readback adds no acetate point; a reversed-range replacement is rejected while the previous valid table remains. Evidence: `.run/remediation-20260925/residual-correction/production-zero.xml` and persisted `cases/.../{real_zero,host,unavailable}/` artifacts. The six-file offscreen GUI run passed **136 tests** (`gui.xml`) before the final reversed-range validation; the subsequent affected Host/GUI/science rerun passed **95 tests** (`final-focused.xml`), including all `test_gui_audit_regressions.py` and `test_host_view.py` cases. No GUI layout, SVG/font, GR-2 publication boundary, or figure writer behavior was changed in this correction.
