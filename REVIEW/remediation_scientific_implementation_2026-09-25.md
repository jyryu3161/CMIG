# Scientific audit remediation: lane A implementation

Date: 2026-09-25  
Baseline: `0167529`  
Scope: SC-01–SC-08 in `REVIEW/remediation_design_2026-09-25.md`; no abundance, cost, or synergy feature work.

## Disposition and evidence

| Finding | Disposition | Evidence |
| --- | --- | --- |
| SC-01, host target identifiability | Fixed. Optimal objective and target identifiability are independent. KO and host-search preserve null point, finite FVA interval, reason/state, and conservative marginal delta interval; only identified points enter transfer/weighted scores. Explicit finite `HostArm.target_transfer` remains a legacy point assertion. | `tests/test_remediation_host_cli.py` uses real Gurobi host solves for baseline `[0,5]` and KO `[5,5]`, FVA failure, all host-search metrics, and JSON/CSV readback. Original `/tmp/cmig_host_ambiguity_probe_20260925.py` now reports null baseline target and null point delta while retaining objective delta `0`. |
| SC-02, hidden community supply | Fixed. Community dFBA closes all undeclared MICOM boundary suppliers, including member sink/demand, preserves explicit formula `X` exemption, rejects forced supply, and diagnoses actual same-solution hidden supply and biomass basis when closure is disabled. Incomplete boundary readout fails acceptance. | Real MICOM/Gurobi sink and demand cases in `tests/test_remediation_scientific.py`; original `/tmp/cmig_scientific_probes_20260925.py` shows sink-supported growth with closure off, no growth with closure on. |
| SC-03, physical exchange orientation | Fixed. Shared one-metabolite exchange identity converts signed reaction flux and physical uptake by stoichiometric coefficient; dFBA and host use physical orientation while COBRA medium bounds retain their native reaction-flux semantics. | Real Gurobi reverse and nonunit standalone dFBA and adaptive clamp cases; host uptake orientation case in `tests/test_remediation_scientific.py`. |
| SC-04, topology identity and member weighting | Fixed. Exchange-to-metabolite maps, MICOM shared-pool/member readout, model import, effective medium, and duplicate member exchanges use actual topology. Community flux normalization applies abundance once. MICOM 0.39 input with unsupported nonunit exchange stoichiometry is rejected explicitly before assembly; validation cache includes file size/mtime. | Renamed exchange, duplicate alias/aggregation, medium ambiguity, normalization, and nonunit rejection in `tests/test_remediation_scientific.py`; original scientific probe identifies `EX_carbon_e` as glucose. |
| SC-05, partial Pareto | Fixed. Capability and every epsilon slice get a serializable attempt record; optimal points survive later timeout/error; infeasible is resolved, cancellation propagates, candidate sampling state is separate from point optimality, and the Pareto checkpoint identity is versioned. Both direct and controlled/service paths preserve ledgers; summary/artifact status and digest include them. | `tests/test_remediation_pareto.py`: 1 optimal/15 **injected** timeouts, injected exceptions, all timeout, infeasible/duplicates, cancellation/resume, real MICOM worker 1/2 equivalence, and CLI artifact integrity. Injected timeout probes do not claim to reproduce a solver timeout. |
| SC-06, legacy membership | Fixed. `rank_consortia` validates requested abundance/IDs and effective MICOM members before ranking, with both memberships in mismatch errors. | `tests/test_remediation_scientific.py` and existing search suites; original `/tmp/cmig_search_probes_20260925.py` now stops on its tiny-member mismatch as required. |
| SC-07, degenerate statistics | Fixed. Undefined Cohen's d, statistic and p-value are nullable with diagnostic/status; strict CLI JSON serializes null rather than NaN/Infinity; volcano input rejects unavailable effect. A constant group with positive pooled variance retains its defined effect. | `tests/test_remediation_stats_solver.py` covers distinct/identical constants, sample sizes 0/1, nonfinite inputs, ordinary variance and CLI sweep. |
| SC-08, solver capability | Fixed. Package presence and COBRA/optlang adapter availability are separate; native HiGHS is unavailable without its adapter and `_require_lp` rejects it. Gurobi license remains a separate solve prerequisite. | Mocked package/adapter states and tiny real Gurobi solve in `tests/test_remediation_stats_solver.py`. |

## Files changed

- Scientific core: `cmig/core/{exchange,dfba,dfba_community,engine,host_coupling,host_impact,host_ko_impact,host_types,medium_spec,model_pool,search,search_multi,search_product,solver,stats,workflow_manifest}.py`.
- Workflow edges: `cmig/io/model_import.py`, `cmig/service/search_service.py`, `cmig/cli/main.py`.
- New regressions: `tests/test_remediation_{scientific,host_cli,pareto,stats_solver}.py`.
- Existing fixture corrections: `tests/test_{dfba,host_ko_impact,host_medium_strictness}.py`.

No MICOM assembly replacement, source GEM edit, golden tolerance change, dependency/version edit, or Git branch/index/HEAD operation was made by this worker.

## Verification

The following used the already synchronized environment and returned code `0`:

| Command | Result |
| --- | --- |
| `uv run --no-sync pytest -q --junitxml=.run/remediation-20260925/science-lane-a-physics.xml tests/test_remediation_scientific.py tests/test_remediation_host_cli.py tests/test_remediation_stats_solver.py tests/test_boundary_isolation.py tests/test_boundary_isolation_dynamics.py tests/test_round6_boundary_regressions.py tests/test_dfba.py tests/test_round8_community_dfba.py tests/test_round9_dfba_community_cli.py tests/test_host_ko_impact.py tests/test_host_medium_strictness.py tests/test_round7_host_interface.py tests/test_medium_namespace_bridge.py tests/test_medium_spec.py tests/test_sign.py tests/test_namespace_gate.py tests/test_search.py tests/test_stats.py tests/test_single_model.py tests/test_solver_and_cli.py tests/test_model_import.py` | 253 collected; 251 passed, 2 pre-existing skips, 0 failures/errors. JUnit: `.run/remediation-20260925/science-lane-a-physics.xml`. |
| `uv run --no-sync pytest -q --junitxml=.run/remediation-20260925/science-lane-a-search.xml tests/test_remediation_pareto.py tests/test_search_policy_v2.py tests/test_search_execution.py tests/test_search_service.py tests/test_search_ga.py tests/test_search_multi_target_metrics.py tests/test_search_unevaluable_partition.py tests/test_engine_golden.py tests/test_workflow_manifest.py tests/test_run_transaction.py tests/test_inspect_run_workflow_manifest.py` | 253 passed, 0 skips/failures/errors. JUnit: `.run/remediation-20260925/science-lane-a-search.xml`. |
| `uv run --no-sync pytest -q tests/test_remediation_scientific.py::test_hidden_community_supply_is_closed_or_diagnosed tests/test_round9_dfba_community_cli.py` | 6 passed after the final additive `untracked_uptake_basis` output. |
| `uv run --no-sync ruff check` on the 19 owned production and 7 owned test files listed above | `All checks passed!` after sorting 3 import blocks. |
| `uv run --no-sync mypy` on the 19 owned production files listed above | `Success: no issues found in 19 source files`. |
| `git diff --check --` on owned tracked files | Clean. |

The focused suites include real tiny Gurobi and MICOM 0.39 solves where the scientific result matters. The two skips are pre-existing environment-gated cases; they were not changed into passes. Whole-tree release/envelope gates, licensed publication checks, GUI visual checks, and independent Astra acceptance belong to the coordinator's stable-tree validation.

## Additive schema handoff

- Host KO arm/delta JSON carries nullable `target_transfer`, `target_transfer_range`, `target_identifiability`, `target_reason`, `microbe_to_host_ranges`, `metabolite_identifiability`, `target_comparable`, `delta_target_transfer_range`, and `delta_microbe_to_host_ranges` as applicable. CSV uses blank nullable point fields plus `*_lo`/`*_hi`; blank means unknown, not zero. Host-search `top_ranked` and `unevaluated` retain nullable point/range/state and objective. Its scientific `status` degrades when optimal transfer is ambiguous. The GUI owner was notified through the coordinator; GUI follow-up handles range-only keys and blank CSV values.
- Pareto result/summary adds `pareto_attempts`, `candidate_sampling_status`, `sampling_status`, `n_pareto_attempts`, `n_pareto_resolved_attempts`, and `n_pareto_failed_attempts` in their corresponding result/evaluation/summary locations. `search_evaluations.json` carries the digest-covered attempt ledger. A partial candidate can have `optimal` point rows and `partial` sampling; run status is `degraded`.
- Community dFBA summary adds `untracked_uptake_basis`, keyed like `untracked_uptake`: member reactions use `member_biomass`, environmental exchanges use `community_biomass`. `untracked_uptake` remains physical supply read from the same solution.
- Workflow manifest adds readout provenance markers only; fixed run hashes and unrelated single-target checkpoint identity stay unchanged. Pareto checkpoint identity alone includes `pareto_evaluation_policy=attempt_ledger_v2`.

## Limits and handoff

The local probes and tiny real models establish the bounded contracts, not publication validity for real host/community data. In particular, interval delta bounds are conservative marginal subtraction, not a jointly feasible paired range, and partial Pareto attempts do not certify an exhaustive frontier. Figure work F2/F9 remains assigned to lane D after the coordinator receives explicit `cmig/cli/main.py` release; GUI handling of null host readouts remains assigned to lane B. Root owns staging, commits, push, global gates, and final acceptance.

## Independent Astra R1–R5 correction — 2026-09-25

This section records the follow-up to `REVIEW/remediation_scientific_review_2026-09-25.md`. The earlier disposition and test totals above describe the pre-review implementation; the corrections here supersede them for R1–R5. The independent review report was not edited.

| Review item | Correction and checked behavior |
| --- | --- |
| R1/R2, member kinetic caps | Community dFBA now derives each channel's shared-pool coefficient after setting the current abundance. For a member channel, `q=(c_pool/abundance)*raw_flux` is the signed member-biomass pool amount. A nonnegative auxiliary for each channel bounds gross uptake, and one sum constraint enforces the member/nutrient Michaelis budget across all channels. The environmental cap uses the abundance-weighted budgets once. Original channel bounds remain in force; no duplicate is discarded or assigned a fixed share. The no-override fallback Vmax uses physical supplying coordinates. Actual MICOM/Gurobi zero-cap forward/reverse runs both keep M0 at biomass 0.1 and zero uptake. One versus two channels at Vmax 2 both yield M0 uptake 1.98019802 on the first step. Mixed orientation, a blocked channel with a usable alternative, unequal abundance and two integration steps pass per-member uptake and concentration-balance assertions. |
| R3, standalone dFBA artifact | `DfbaResult.untracked_uptake_basis` now derives `model_biomass` for each actual untracked boundary rate. This pairs the existing CLI serializer with an honest standalone denominator. Both previously failing real standalone CLI tests pass. No CLI plot/export code was edited. |
| R4, collapsed host zero | Per-metabolite point attribution now uses the same finite collapsed-range rule as the target. An actual host LP/FVA `[0,0]→[5,5]` has identified acetate in both arms and per-metabolite delta `+5` with marginal interval `[5,5]`; the reverse is `−5`. Proven structural zero is retained in the per-metabolite range map. The real JSON/CSV writer preserves numeric zero and `+5`; noncollapsed ranges still yield null points. |
| R5, truthful Pareto attempts | Each capability-pass baseline and independent target LP has its own phase, target/direction when applicable, raw status, outcome, growth floor/policy, and measured readout. The sampled-pass baseline and every slice remain separate. Independent capability targets carry a scalar measured result and **no** achieved joint vector; only a solved joint slice has `achieved_vector`. The instrumented two-target COBRA case has 20 actual optimizations and 20 ledger rows (2 baselines, 2 capabilities, 16 slices), including real infeasible statuses. Failed capability sampling is `failed` in direct, controlled and checkpoint-resume paths; a repeated failed capability record is not double-counted. Injected capability cancellation propagates in both direct and controlled paths. Earlier feasible slices survive later injected timeout/error and worker/checkpoint transfer. Pareto checkpoint identity is now `attempt_ledger_v3`, so v2 histories cannot be silently certified under the new ledger policy. |

The R5 end-to-end failed-run test exposed an additional artifact declaration mismatch: a no-rank multi-target run correctly omitted plots, but its summary/manifest still declared both plot files, so Search staging discarded the failed ledger despite a matching digest. After the coordinator authorized this bounded scientific CLI section, `_write_multi_target_outputs` now declares figure artifacts only when ranked rows actually produce them. It still invokes the existing plot function for valid ranked runs and retains strict digest verification. The injected capability-timeout CLI case with explicit `--allow-failed-run` now publishes a **scientifically failed** summary, three actual baseline/capability attempt records, no nonexistent plot names, and a verified result digest with no missing artifacts. The zero exit code in that case follows the explicit artifact-publication flag; it does not mean scientific success.

`n_pareto_attempts` and downstream summary counts now derive from the separate records; for the reviewed toy, the published attempt count changes from 17 to 20. Record `solve_executed` distinguishes actual LP calls from a missing-target or setup outcome. Timeout and cancellation regressions use explicit injection and do not claim a real solver timeout or mid-optimization interrupt. The coordinator was notified of the public attempt-count change and of CLI ownership release for the separate figure correction.

Evidence is under `.run/remediation-20260925/science-correction/`: `focused.log`, `focused.xml`, `failed-cli-diagnostic.log`, and `commands.txt`; the existing ignored independent probes were rerun and their results remain under `.run/remediation-20260925/science-review/`. The bounded focused command returned **174 passed, 0 failed, 0 skipped** across the scientific/solver/service files named in `commands.txt`; after the final CLI declaration edit, **64 Pareto/figure/search regressions passed**, and the final host rerun had **24 passed**. Scoped Ruff, `mypy cmig` (91 source files), release/version guard, 18-kind envelope guard and scoped `git diff --check` passed. This remains tiny LP and software-contract evidence, not a publication-validity claim for large GEMs, native Windows, GUI visuals, or remote CI. No dependency pin, golden tolerance, source GEM, Git index, commit or push was changed by this correction.

## Residual RR1/RR2 correction — 2026-09-25

This section follows `REVIEW/remediation_scientific_rereview_2026-09-25.md`; the earlier R4/R5 claims above are limited by the two residuals until this correction. The independent review report and its evidence were not edited.

| Residual | Corrected public behavior |
| --- | --- |
| RR1, sampled frontier baseline exception | `_community_growth_star` reports entry to the optimize boundary. An exception before any baseline observer row now records `phase=baseline`, the growth policy floors, raw `solver_error`, the exception diagnostic, `solve_started=true`, `solve_executed=null`, and no measured result; prior capability attempts remain intact. A setup failure before that boundary remains `phase=setup`, `solve_started=false`, `solve_executed=false`. If the observer already recorded an optimal solver return but subsequent growth readout fails, the same baseline row keeps `raw_status=optimal` and `solve_executed=true` while its outcome becomes `error` with the raw readout exception. Neither case ranks the failed candidate or reports a fabricated solved scientific point. Direct and controlled real LP regressions cover exceptions before and after the observer. |
| RR2, finite host transfer interval | `identified_transfer_point` is shared by HostArm/KO arithmetic, the Host CSV writer and GUI readout. For a trustworthy optimal solve, a finite ordered interval with width at most `1e-6` identifies its midpoint, including zero; a broader interval overrides any incidental point and remains unknown. The producer keeps its positive-only transfer map for edges. The actual acetate-disabled host LP yields an FVA range `[0,0]` with no sparse map entry, yet `microbe_to_host.csv` now writes `ac,0,0,0,True`; the saved JSON retains the sparse map and measured range. Real `[0,5]` and FVA-failure cases retain null point behavior, and the existing `[0,0]→[5,5]` plus reverse KO delta tests remain green. |

Evidence is under `.run/remediation-20260925/residual-correction/`. The broad scoped science/search/Host command in `verification.md` passed **214 tests** (`science.xml`) before the final narrow readout refinement. The affected science/GUI command after that refinement passed **95 tests** (`final-focused.xml`), and the final after-observer Pareto command passed **24 tests** (`pareto-final.xml`). The persisted real LP→writer→GUI case passed again (`production-zero.xml`); its CSV, JSON and verified manifest are under `cases/.../real_zero/`. The full scoped GUI command passed **136 tests** (`gui.xml`) before the final ordered-range validation, which is covered by the 95-test affected rerun. Scoped Ruff, `mypy cmig` (91 source files), version guard, 18-kind envelope guard and `git diff --check` passed. No whole-suite rerun, biological efficacy or publication-validity claim is made here.

## Integration fixture and guide follow-up — 2026-09-25

The independent `REVIEW/remediation_integration_triage_2026-09-25.md` identified ten stale or incomplete fixtures in five legacy test files. The five files now provide the required shared-pool compartments, explicit private readout community, physically balanced MICOM-shaped flux metadata, identified-zero Host status evidence, measured butyrate-zero interval, and typed coherent `HostImpact` for optimal Host search. Original run-level warning, pFBA fallback, failed-LP ranking and figure assertions remain, alongside new weighted-balance, ambiguous/unavailable objective-only and sparse-absence controls. No production algorithm or solver constraint was changed for these failures.

`docs/USAGE.md` and `docs/USER_GUIDE.md` now describe Pareto counts as **all attempts**, including both baselines, independent capabilities, sampled slices and any minimization auxiliary. They give the ordinary two-target example as **20 total / 16 sampling slices**, define resolved as optimal/infeasible and failed as timeout/error, and explain that other outcomes need not belong to either subtotal. The guide states that `attempt_ledger_v2` checkpoint histories are incompatible with the `attempt_ledger_v3` policy even when the older history already has a ledger.

The complete five-file command passed **133 tests, zero failures/skips** (`.run/remediation-20260925/integration-fixture-correction/five-files-final.xml`). Ruff on all five changed test files and scoped `git diff --check` passed. The evidence directory's `verification.md` lists the seven source/documentation files and exact changes. Root owns the stable-tree full suite and broader quality gates; this follow-up made no environment, dependency, Git index, commit or push change.
