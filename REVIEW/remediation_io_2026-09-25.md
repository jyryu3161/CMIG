# IO, package, and CI remediation — 2026-09-25

Implementation owner: GPT-6 Sol. Base commit: `0167529fa9ba81649b138c476c524dc4495d4954`. Scope follows `AGENTS.md`, `REVIEW/CMIG_full_audit_2026-09-25.md`, F1/F11/F12 in `REVIEW/ui_workflow_audit_2026-09-25.md`, lane C/C3 in `REVIEW/remediation_design_2026-09-25.md`, and the coordinator's explicit addition of `scripts/audit_distribution.py`. The post-review correction is recorded below. No branch, staging, commit, push, shared `.venv` mutation, or live publisher request was made.

## Dispositions

| Finding | Change | Regression/evidence |
| --- | --- | --- |
| F1 catalogue ID and output escape | Validate IDs and `.xml` filenames at parse, catalogue read/write, and fetch. Reject separators, drives, URL metacharacters/encoding and controls. Resolve the chosen output root, reject symlink targets, publish direct bytes atomically, and use exclusive unpredictable conversion staging names. The post-review shared atomic correction also supports platforms without `os.fchmod` while retaining POSIX mode preservation. Existing output and outside sentinels survive invalid entries and conversion failures. | Offline `tests/test_agora2.py` direct/cached malicious entries, symlink destination for SBML/JSON, old predictable staging symlink, prior output on invalid conversion, and real SBML publication with `os.fchmod` absent. `tests/test_atomic_portability.py` covers all three atomic writer forms, replacement failure preservation, staging cleanup, and POSIX modes. The original temporary-directory probe changed `original user data` to `<sbml/>` before the fix. |
| F11 publisher URL and redirects | Parse exact HTTPS origin without credentials/port/query/fragment, allow only the index, individual `.xml` route and known archive URLs, and reject dot/encoded path escapes. A guarded redirect handler validates each target before the next transport request; the final URL is checked and `FetchedModel.effective_url` records allowed redirects while `source_url` retains the requested URL. | Entirely offline fake HTTPS handler proves relative same-origin redirect, off-origin/scheme/path/encoded rejection before the target request, a redirect loop failure, and final URL mismatch. The former live-network regression now rejects hostile filenames before its fetcher is called. |
| F12 installed presets and sdist tutorial | Copy the existing `medium_presets/` tree, including source and row provenance, byte-for-byte into `cmig/resources/medium_presets/`; `importlib.resources` is the default GUI discovery/load route while `preset_dir=` remains supported. README tutorial now uses its source URL, and README documents installed GUI presets versus source-checkout CLI paths. Distribution audit requires wheel preset/provenance members and checks sdist README local links. | Fresh sdist and wheel built from it pass archive audit. A real wheel `uv pip install --target` outside the checkout loaded 10 combo entries, the 133-row AGORA western overlay, and its `nutrient` row role; `cmig.__file__` pointed to `/tmp/cmig-io-install-gylU8V/site/cmig/__init__.py`. Source/package bytes are checked in `tests/test_distribution_audit.py`. |
| CI Python 3.10 `tomllib` import | Use `tomllib` on 3.11+ and `tomli` on 3.10; add a Python<3.11 dev dependency. Offline `uv lock` changed only the two `cmig` dev dependency entries; existing `tomli==2.4.1` resolution and `micom==0.39.0` remain unchanged. | Fresh isolated Python 3.10.18 venv with offline `tomli==2.4.1` ran the real tagged release guard successfully; fallback regression also passes. |
| CI Windows locale read and tag output | Every repository text read in `check_release_versions.py` explicitly uses UTF-8. The tag status line uses ASCII `->` so a cp1252 console can print it. | Tests enforce explicit encodings, parse a non-ASCII TOML fixture with a simulated cp1252 default, run tag output with `PYTHONIOENCODING=cp1252:strict`, and retain deliberate version/tag mismatch rejection. Native Windows CI has not run on this unpushed tree. |
| CI strict mypy PySide provisioning | Quality matrix sync includes `--extra gui` and subsequent `uv run --no-sync` commands preserve that provisioned environment. `mypy cmig` remains strict with no new ignores. | The baseline GitHub log's macOS/Ubuntu `QWidget`/PySide-as-Any errors followed omission of the GUI extra. Local owned-file mypy passes with installed PySide; full-tree quality awaits the coordinator after peer edits settle. |

## Commands and results

- Reproduction: `uv run --no-sync python -c '<TemporaryDirectory F1 probe>'` printed `<sbml/>` for a sibling sentinel, confirming the audited pre-fix overwrite. Baseline CI evidence in `.run/remediation-20260925/baseline-ci-failures.log` independently showed Python 3.10 `ModuleNotFoundError: tomllib`, Windows `UnicodeDecodeError` under cp1252, and 20 PySide-as-Any strict mypy errors.
- Initial pre-review lane run: `QT_QPA_PLATFORM=offscreen uv run --no-sync pytest -o addopts='' -q tests/test_agora2.py tests/test_distribution_audit.py tests/test_release_versions.py tests/test_gui_editors_builder.py tests/test_round9_gui_medium_tools.py tests/test_medium_presets_gut.py` — exit 0, **95 passed, 0 failed, 0 skipped**. The corrected current-tree checks appear below.
- `uv run --no-sync ruff check cmig/io/agora2.py cmig/gui/editors.py scripts/audit_distribution.py scripts/check_release_versions.py tests/test_agora2.py tests/test_distribution_audit.py tests/test_release_versions.py` — exit 0. `uv run --no-sync mypy cmig/io/agora2.py cmig/gui/editors.py` — exit 0, two source files checked. `uv lock --check --offline`, `git diff --check`, and `uv run --no-sync python scripts/check_release_versions.py v0.3.0` — exit 0.
- `uv venv --python 3.10 .run/remediation-20260925/io-py310`, `uv pip install --offline --python .run/remediation-20260925/io-py310/bin/python tomli`, then `.run/remediation-20260925/io-py310/bin/python scripts/check_release_versions.py v0.3.0` — exit 0; isolated environment only.
- `uv build --out-dir .run/remediation-20260925/io-dist-final` — exit 0, built sdist then wheel from that sdist. `uv run --no-sync python scripts/audit_distribution.py .run/remediation-20260925/io-dist-final/*` — exit 0 for both archives.
- `uv pip install --target /tmp/cmig-io-install-gylU8V/site --no-deps .run/remediation-20260925/io-dist-final/cmig-0.3.0-py3-none-any.whl` — exit 0. Offscreen `.venv/bin/python` from `/tmp/cmig-io-install-gylU8V/outside` with only that target on `PYTHONPATH` — exit 0, printed package import path, 10 choices, 133 rows, first role `nutrient`, and persistent installed preset path.

## Limits and follow-up

No live AGORA publisher behavior or native Windows runner was exercised. The allowed publisher route is intentionally limited to the known index, individual reconstructions and two named archives; a future publisher path change needs an explicit reviewed update. The resource copy is guarded by a byte equality regression so future source preset changes require synchronized package assets. URL/output validation and atomic replacement protect these application boundaries but do not constitute an adversarial cross-process filesystem sandbox. The coordinator owns final integrated quality/full-suite verification and Git publication; remote solver CI remains subject to absent Gurobi WLS secrets, as the coordinator reported.

## Correction after independent review

The [independent IO review](remediation_io_review_2026-09-25.md) requested one F1 correction: the direct VMH/SBML branch now used `atomic_write_bytes`, whose unconditional `os.fchmod` call fails when that Unix-only API is absent. A failing regression reproduced the `AttributeError` through public `fetch_model` and all three shared atomic writer forms with the API removed. `cmig/io/atomic.py` now applies descriptor mode only where supported; the existing temporary-file creation, file sync, same-directory replacement and error cleanup remain in place. The real minimal SBML fetch regression verifies the final bytes, and the helper regressions verify replacement and staging cleanup, previous destination bytes after injected replacement failure, and preserved POSIX mode for binary, text and path writers.

Corrected current-tree checks: `uv run --no-sync pytest -o addopts='' -q tests/test_agora2.py tests/test_atomic_portability.py tests/test_round7_atomic_io.py tests/test_round8_atomic_adoption.py tests/test_model_import.py tests/test_distribution_audit.py tests/test_release_versions.py` — **109 passed, 0 failed, 0 skipped**. Scoped Ruff on these tests and `cmig/io/{atomic,agora2}.py`, scoped mypy on those two source files, `uv lock --check --offline`, and `python scripts/check_release_versions.py v0.3.0` all passed. Fresh direct wheel and sdist-to-wheel builds passed `scripts/audit_distribution.py`; each wheel contained the corrected `atomic.py`, was installed with `uv pip install --target --no-deps` in a separate target, and from a source-external working directory successfully imported from that target, found the installed gut preset, and published direct VMH/SBML bytes with `os.fchmod` absent. The new build/install evidence is under `.run/remediation-20260925/io-correction/`.

The portability test removes the API on macOS; it is not a native Windows run. Live publisher behavior, final full-tree checks and remote CI remain coordinator gates. The reported missing Gurobi WLS secrets remain a required external prerequisite and were neither supplied nor treated as a passing skip.
