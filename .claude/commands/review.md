# CMIG Astra review

Use `gpt-6-astra` for read-only review. Read `AGENTS.md`, the phase acceptance criteria, changed code and runner-owned check evidence. Report reproducible findings, severity, file/line evidence and coverage limits. Return `pass`, `changes_requested` or `blocked` with a Markdown report. Route requested fixes to `gpt-6-sol`; do not make implementation edits during review.
